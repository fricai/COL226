program bad3 ::
 var A : int;
 var unknownmothergoose:
                                                    bool;
 {
  if (5 < 3) then {
   A := unknownmothergoose;
  } else {
   C := D;
   while (P < Q) do {
     X := Y;
   } endwh;
  } endif;
  A := 1 < tt;
 }
