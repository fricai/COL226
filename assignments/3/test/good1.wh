program good1 ::
 var A : int;
 var unkownmothergoose:
                                                    bool;
 {
  if (5 < 3) then {
   A := unknownmothergoose;
  } else {
   C := D;
   while (P < Q) do {
     X := Y;
   } endwh;
  } endif;
  A := 1 < tt;
 }
