program lol ::
 var A : int;
 var B:
                                                    bool;
 {
  if (5 < 3) then {
   A := B;
  } else {
   C := D;
   while (P < Q) do {
     X := Y;
   } endwh;
  };
  A := 1 < tt;
 }
