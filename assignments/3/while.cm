Library
   structure While
is
   datatypes.sml      (* Lots of datatypes *)
   while.lex             (* Lexer rules. *)
   while.yacc:MLYacc     (* Parser rules. *)
   glue.sml           (* Build the parser *)
   compiler.sml       (* Lex, parse, panic... *)

   $/basis.cm         (* Basis library *)
   $/ml-yacc-lib.cm   (* ML-Lex and ML-Yacc library *)
   $smlnj/compiler/compiler.cm    (* Structure Compiler, needed
                                     for printDepth, autoloaded. *)
